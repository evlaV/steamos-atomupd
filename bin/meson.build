install_data('steamos-update-client', install_dir: bindir)

steamos_update_client = join_paths(meson.source_root(), 'bin', 'steamos-update-client')
run_target('run',
  command: steamos_update_client,
)

